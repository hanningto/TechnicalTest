'''Write a program that accepts a string as input, capitalizes the first letter of each word in the
string, and then returns the result string.'''

stat = input("Enter a statement")# takes the user input
cap_stat = stat.title()#the title() method capitalizes the first letters of each word and it is saved at variable cap_stat
print(cap_stat)# prints the resultant statment

